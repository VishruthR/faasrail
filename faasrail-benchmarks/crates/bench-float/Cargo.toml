[package]
name = "bench-float"
version = "0.1.0"
edition = "2015"

[lib]
name = "bench_float"
path = "src/lib.rs"

[dependencies]
serde = { version = ">=1.0, <1.0.100", features = ["derive"] }
serde_json = ">=1.0, <1.0.56"
serde_derive = ">=1.0, <1.0.100"
ryu = ">=1.0, <1.0.10"
