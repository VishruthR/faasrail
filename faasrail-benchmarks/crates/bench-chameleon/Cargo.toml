[package]
name = "bench-chameleon"
version = "0.1.0"
edition = "2018"

[dependencies]
bench-common = { path = "../bench-common" }
serde = { version = ">=1.0, <1.0.100", features = ["derive"] }
serde_json = ">=1.0, <1.0.56"
