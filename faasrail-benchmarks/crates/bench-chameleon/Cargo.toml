[package]
name = "bench-chameleon"
version = "0.1.0"
edition = "2015"

[lib]
name = "bench_chameleon"
path = "src/lib.rs"

[dependencies]
serde = { version = ">=1.0, <1.0.100", features = ["derive"] }
serde_json = ">=1.0, <1.0.56"
serde_derive = ">=1.0, <1.0.100"
