[package]
name = "bench-chameleon"
version = "0.1.0"
edition = "2015"

[lib]
name = "bench_chameleon"
path = "src/lib.rs"

[dependencies]
# Pinned for OpenWhisk rust:1.34 (Cargo must not parse registry crates using edition 2021+).
serde = "=1.0.91"
serde_json = "=1.0.39"
serde_derive = "=1.0.91"
